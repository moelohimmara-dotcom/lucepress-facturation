import DashboardLayout from "@/components/DashboardLayout";
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { AlertTriangle, Eye, Inbox, Loader2, MailCheck, Search, ShieldCheck } from "lucide-react";
import { useMemo, useState } from "react";
const formatDate = (value: Date | string | null) => value ? new Date(value).toLocaleString("fr-FR", { day: "2-digit", month: "short", year: "numeric", hour: "2-digit", minute: "2-digit" }) : "Non remis";
export default function AgentTestEmailPage() { const { data: center, isLoading, error } = trpc.billing.agent.center.useQuery(); const [search, setSearch] = useState(""); const [current, setCurrent] = useState<any | null>(null); const deliveries = useMemo(() => { const text = search.trim().toLowerCase(); return (center?.testEmailDeliveries ?? []).filter((delivery: any) => !text || `${delivery.clientName} ${delivery.documentNumber} ${delivery.subject} ${delivery.campaignName}`.toLowerCase().includes(text)); }, [center?.testEmailDeliveries, search]); if (isLoading) return <DashboardLayout><div className="flex min-h-[60vh] items-center justify-center"><Loader2 className="h-6 w-6 animate-spin text-primary" /></div></DashboardLayout>; if (error) return <DashboardLayout><div className="mx-auto max-w-3xl py-10"><Alert variant="destructive"><AlertTriangle className="h-4 w-4" /><AlertTitle>Accès réservé</AlertTitle><AlertDescription>{error.message}</AlertDescription></Alert></div></DashboardLayout>; return <DashboardLayout><main className="mx-auto max-w-5xl pb-10"><header className="border-b border-border pb-6"><p className="text-xs font-extrabold uppercase tracking-[0.18em] text-primary">Agent IA · Canal de test</p><h1 className="font-editorial mt-2 text-3xl font-semibold">Boîte e-mail de test</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-muted-foreground">Consultez la copie interne des messages générés. Cette boîte ne remet aucun courrier à un client, à une adresse e-mail ou à un service externe.</p></header><section className="mt-6 rounded-2xl border border-primary/15 bg-primary/[0.035] p-5"><div className="flex gap-3"><ShieldCheck className="mt-0.5 h-5 w-5 shrink-0 text-primary" /><div><h2 className="text-sm font-extrabold">Connecteur isolé de prévisualisation</h2><p className="mt-1 text-xs leading-5 text-muted-foreground">Chaque livraison marquée « remis en test » est une copie stockée dans l’application. L’activation d’un fournisseur e-mail réel nécessitera une intégration distincte, des secrets serveur et une validation humaine.</p></div></div></section><section className="mt-6 rounded-2xl border border-border bg-card p-4"><div className="relative"><Search className="absolute left-3 top-3 h-4 w-4 text-muted-foreground" /><Input value={search} onChange={event => setSearch(event.target.value)} className="h-10 pl-9" placeholder="Rechercher un client, un document ou une campagne" /></div></section><section className="mt-6 overflow-hidden rounded-2xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border bg-secondary/60 px-5 py-3"><div className="flex items-center gap-2"><Inbox className="h-4 w-4 text-primary" /><span className="text-xs font-extrabold">{deliveries.length} message(s) de test</span></div><Badge variant="outline" className="border-emerald-200 bg-emerald-50 text-emerald-800">Aucun envoi externe</Badge></div>{deliveries.length ? deliveries.map((delivery: any) => <article key={delivery.id} className="flex flex-col gap-3 border-b border-border p-4 last:border-0 sm:flex-row sm:items-center sm:justify-between sm:px-5"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><p className="truncate text-sm font-extrabold">{delivery.subject}</p><Badge variant="outline" className="border-emerald-200 bg-emerald-50 text-emerald-800">Remis en test</Badge></div><p className="mt-1 text-xs text-muted-foreground">{delivery.clientName} · {delivery.documentNumber} · {delivery.campaignName}</p><p className="mt-1 text-[11px] text-muted-foreground">Boîte : {delivery.testRecipient} · {formatDate(delivery.deliveredAt)}</p></div><Button size="sm" variant="outline" className="w-fit rounded-lg border-primary/20 text-primary" onClick={() => setCurrent(delivery)}><Eye className="mr-1.5 h-3.5 w-3.5" />Ouvrir</Button></article>) : <div className="p-10 text-center"><MailCheck className="mx-auto h-7 w-7 text-primary" /><p className="mt-3 text-sm font-extrabold">La boîte de test est vide.</p><p className="mx-auto mt-1 max-w-md text-xs leading-5 text-muted-foreground">Lancez un test depuis le planificateur après avoir préparé et activé une campagne e-mail en simulation.</p></div>}</section></main><Dialog open={Boolean(current)} onOpenChange={open => !open && setCurrent(null)}><DialogContent className="sm:max-w-xl"><DialogHeader><DialogTitle>Prévisualisation e-mail de test</DialogTitle><DialogDescription>Copie interne remise dans la boîte de test, sans transmission externe.</DialogDescription></DialogHeader>{current && <div className="space-y-3"><div className="rounded-xl bg-secondary/60 p-3 text-xs"><p><strong>Campagne :</strong> {current.campaignName}</p><p className="mt-1"><strong>Client :</strong> {current.clientName} · {current.documentNumber}</p><p className="mt-1"><strong>Objet :</strong> {current.subject}</p></div><pre className="max-h-[48vh] overflow-auto whitespace-pre-wrap rounded-xl border border-border bg-background p-4 font-sans text-sm leading-6">{current.body}</pre></div>}</DialogContent></Dialog></DashboardLayout>; }
