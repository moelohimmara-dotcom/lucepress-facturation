import { useAuth } from "@/_core/hooks/useAuth";
import { Button } from "@/components/ui/button";
import { startLogin } from "@/const";
import { downloadPdfFromElement } from "@/lib/pdf";
import { trpc } from "@/lib/trpc";
import { formatGnf } from "@shared/billing";
import { LUCEPRES_PUBLIC_PROFILE } from "@shared/companyProfile";
import { ArrowLeft, CalendarDays, CheckCircle2, Download, FileText, Loader2, ReceiptText, ShieldCheck, WalletCards } from "lucide-react";
import React, { useState } from "react";
import { toast } from "sonner";

function formatDate(value: Date | string | null) { return value ? new Date(value).toLocaleDateString("fr-GN") : "Non renseignée"; }

export default function ClientPortalPage() {
  const { user, loading, isAuthenticated } = useAuth();
  const [selectedInvoiceId, setSelectedInvoiceId] = useState<number | null>(null);
  const { data: overview, isLoading } = trpc.billing.clientPortal.overview.useQuery(undefined, { enabled: isAuthenticated });
  if (loading) return <PortalShell><div className="flex min-h-[60vh] items-center justify-center"><Loader2 className="h-7 w-7 animate-spin text-primary" /></div></PortalShell>;
  if (!user) return <PortalShell><section className="mx-auto max-w-md py-16 text-center"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-secondary text-primary"><ShieldCheck className="h-7 w-7" /></div><h1 className="font-editorial mt-5 text-3xl font-semibold">Consultez vos factures</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">Connectez-vous avec l’adresse e-mail renseignée sur votre fiche client pour accéder uniquement à votre historique Lucepres.</p><Button onClick={() => startLogin()} className="mt-7 h-11 rounded-xl bg-primary font-bold text-primary-foreground">Accéder à mon portail</Button></section></PortalShell>;
  if (selectedInvoiceId) return <ClientInvoiceDetail invoiceId={selectedInvoiceId} onBack={() => setSelectedInvoiceId(null)} />;
  return <PortalShell><main className="mx-auto max-w-5xl pb-10"><header className="mb-7 flex flex-col gap-4 border-b border-border pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-extrabold uppercase tracking-[0.2em] text-primary">Espace client sécurisé</p><h1 className="font-editorial mt-2 text-3xl font-semibold">Mes factures</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Consultez vos documents et téléchargez leur version PDF. Cet espace est en lecture seule.</p></div><div className="rounded-xl border border-primary/15 bg-primary/[0.035] px-4 py-3 text-xs font-semibold text-primary">Connecté : {user.email ?? "e-mail non renseigné"}</div></header>
    {isLoading ? <div className="flex min-h-[40vh] items-center justify-center"><Loader2 className="h-7 w-7 animate-spin text-primary" /></div> : !overview?.client ? <section className="card-shadow rounded-2xl border border-border bg-card p-8 text-center"><ShieldCheck className="mx-auto h-8 w-8 text-primary" /><h2 className="font-editorial mt-4 text-2xl font-semibold">Compte client à associer</h2><p className="mx-auto mt-3 max-w-lg text-sm leading-6 text-muted-foreground">Aucune fiche client ne correspond à l’adresse e-mail utilisée. Contactez Lucepres pour faire associer votre adresse à votre dossier.</p></section> : <><section className="rounded-2xl border border-primary/15 bg-primary/[0.035] p-5"><p className="text-xs font-extrabold uppercase tracking-[0.16em] text-primary">Dossier client</p><p className="font-editorial mt-2 text-2xl font-semibold">{overview.client.companyName}</p><p className="mt-2 text-xs text-muted-foreground">Les documents affichés sont strictement limités à ce dossier client.</p></section><section className="card-shadow mt-6 overflow-hidden rounded-2xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border p-5"><div><h2 className="font-editorial text-xl font-semibold">Historique des factures</h2><p className="mt-1 text-xs text-muted-foreground">{overview.invoices.length} facture{overview.invoices.length > 1 ? "s" : ""} disponible{overview.invoices.length > 1 ? "s" : ""}</p></div><ReceiptText className="h-5 w-5 text-primary" /></div>{overview.invoices.length ? <div className="divide-y divide-border">{overview.invoices.map(invoice => <article key={invoice.id} className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex flex-wrap items-center gap-2"><p className="font-mono text-sm font-extrabold text-primary">{invoice.number}</p><span className={`rounded-full px-2.5 py-1 text-[10px] font-extrabold ${invoice.balanceDue > 0 ? invoice.isOverdue ? "bg-red-100 text-red-800" : "bg-amber-100 text-amber-800" : "bg-emerald-100 text-emerald-800"}`}>{invoice.balanceDue > 0 ? invoice.isOverdue ? "En retard" : "Solde à régler" : "Réglée"}</span></div><p className="mt-2 text-xs text-muted-foreground">Émise le {formatDate(invoice.issueDate)} · Échéance : {formatDate(invoice.dueDate)}{invoice.projectName ? ` · ${invoice.projectName}` : ""}</p></div><div className="flex items-center justify-between gap-4 sm:justify-end"><div className="text-right"><p className="font-mono text-sm font-extrabold">{formatGnf(invoice.total)}</p>{invoice.balanceDue > 0 && <p className="mt-1 text-[11px] font-semibold text-primary">Solde : {formatGnf(invoice.balanceDue)}</p>}</div><Button onClick={() => setSelectedInvoiceId(invoice.id)} className="h-9 rounded-lg bg-primary text-xs font-bold text-primary-foreground"><FileText className="mr-2 h-4 w-4" />Consulter</Button></div></article>)}</div> : <div className="p-10 text-center"><ReceiptText className="mx-auto h-7 w-7 text-primary/50" /><p className="mt-3 text-sm font-extrabold">Aucune facture disponible</p><p className="mt-1 text-xs text-muted-foreground">Vos prochaines factures apparaitront ici après émission par Lucepres.</p></div>}</section></>}</main></PortalShell>;
}

function ClientInvoiceDetail({ invoiceId, onBack }: { invoiceId: number; onBack: () => void }) {
  const { data: invoice, isLoading } = trpc.billing.clientPortal.invoice.useQuery({ id: invoiceId });
  const [isExporting, setIsExporting] = useState(false);
  async function downloadPdf() { setIsExporting(true); try { await downloadPdfFromElement("client-portal-invoice", `facture-${invoice?.number ?? invoiceId}`); toast.success("Votre facture PDF a été téléchargée."); } catch { toast.error("Le PDF n’a pas pu être généré. Réessayez dans un instant."); } finally { setIsExporting(false); } }
  return <PortalShell><main className="mx-auto max-w-5xl pb-10"><header className="mb-6 flex flex-col gap-4 border-b border-border pb-6 sm:flex-row sm:items-center sm:justify-between"><Button variant="ghost" onClick={onBack} className="w-fit px-0 text-primary"><ArrowLeft className="mr-2 h-4 w-4" />Retour à mes factures</Button>{invoice && <Button onClick={downloadPdf} disabled={isExporting} className="h-10 rounded-xl bg-primary font-bold text-primary-foreground"><Download className="mr-2 h-4 w-4" />{isExporting ? "Génération…" : "Télécharger PDF"}</Button>}</header>{isLoading ? <div className="flex min-h-[55vh] items-center justify-center"><Loader2 className="h-7 w-7 animate-spin text-primary" /></div> : !invoice ? <section className="rounded-2xl border border-border bg-card p-10 text-center"><ShieldCheck className="mx-auto h-7 w-7 text-primary" /><p className="mt-3 text-sm font-extrabold">Cette facture n’est pas disponible pour votre compte.</p></section> : <article id="client-portal-invoice" className="card-shadow overflow-hidden rounded-2xl border border-border bg-white text-[#183a35]"><div className="border-b-[8px] border-[#153f38] p-6 sm:p-10"><div className="flex flex-col gap-6 sm:flex-row sm:items-start sm:justify-between"><div className="flex items-center gap-3"><img src="/manus-storage/lucepress-emblem_bfa24e8e.png" alt="Emblème Lucepres" className="h-14 w-14 rounded-2xl bg-[#edf5f0] p-2" /><div><p className="font-editorial text-3xl font-semibold">{LUCEPRES_PUBLIC_PROFILE.legalName}</p><p className="mt-1 text-[10px] font-extrabold uppercase tracking-[0.16em] text-[#4b746d]">Facture client</p></div></div><div className="sm:text-right"><p className="font-mono text-base font-extrabold text-[#1e6051]">{invoice.number}</p><p className="mt-1 text-xs text-slate-500">Émise le {formatDate(invoice.issueDate)}</p></div></div></div><div className="grid gap-7 p-6 sm:grid-cols-2 sm:p-10"><section><p className="text-[10px] font-extrabold uppercase tracking-[0.16em] text-[#4b746d]">Destinataire</p><p className="mt-2 text-sm font-extrabold">{invoice.clientName}</p>{invoice.contactName && <p className="mt-1 text-sm text-slate-600">{invoice.contactName}</p>}</section><section className="sm:text-right"><p className="text-[10px] font-extrabold uppercase tracking-[0.16em] text-[#4b746d]">Règlement</p><p className="mt-2 text-sm">Échéance : <strong>{formatDate(invoice.dueDate)}</strong></p><p className="mt-1 text-sm">Statut : <strong>{invoice.balanceDue > 0 ? invoice.isOverdue ? "En retard" : "Solde à régler" : "Réglée"}</strong></p></section></div><div className="px-6 pb-6 sm:px-10 sm:pb-10"><div className="overflow-hidden rounded-xl border border-slate-200"><table className="w-full text-left text-xs"><thead className="bg-[#eef5f1] text-[#28534b]"><tr><th className="px-3 py-3">Désignation</th><th className="px-3 py-3 text-right">Qté</th><th className="px-3 py-3 text-right">Total</th></tr></thead><tbody>{invoice.lines.map(line => <tr key={line.id} className="border-t border-slate-200"><td className="px-3 py-3">{line.description}</td><td className="px-3 py-3 text-right">{Number(line.quantity)}</td><td className="px-3 py-3 text-right font-mono font-bold">{formatGnf(line.lineTotal)}</td></tr>)}</tbody></table></div><div className="ml-auto mt-5 max-w-xs space-y-2 border-b border-[#153f38] pb-4 text-sm"><TotalLine label="Total TTC" value={formatGnf(invoice.total)} /><TotalLine label="Déjà encaissé" value={formatGnf(invoice.paidAmount)} /><TotalLine label="Solde dû" value={formatGnf(invoice.balanceDue)} strong /></div>{invoice.payments.length > 0 && <section className="mt-7"><p className="text-[10px] font-extrabold uppercase tracking-[0.16em] text-[#4b746d]">Paiements enregistrés</p><div className="mt-3 divide-y divide-slate-200 rounded-xl border border-slate-200">{invoice.payments.map(payment => <div key={payment.id} className="flex justify-between gap-3 p-3 text-xs"><span>{formatDate(payment.paidAt)}</span><strong className="font-mono text-emerald-700">+ {formatGnf(payment.amount)}</strong></div>)}</div></section>}<footer className="mt-10 flex items-center justify-center gap-2 border-t border-slate-200 pt-5 text-center text-[10px] text-slate-400"><CheckCircle2 className="h-3.5 w-3.5" />Document consulté dans votre espace client sécurisé.</footer></div></article>}</main></PortalShell>;
}

function PortalShell({ children }: { children: React.ReactNode }) { return <div className="surface-grid min-h-screen bg-background text-foreground"><header className="border-b border-border bg-card/90"><div className="mx-auto flex max-w-5xl items-center justify-between gap-3 px-5 py-4"><div className="flex items-center gap-3"><img src="/manus-storage/lucepress-emblem_bfa24e8e.png" alt="Lucepres" className="h-9 w-9 rounded-xl bg-secondary p-1.5" /><div><p className="font-editorial text-lg font-semibold">Lucepres</p><p className="text-[9px] font-extrabold uppercase tracking-[0.15em] text-primary">Portail client</p></div></div><div className="flex items-center gap-1.5 text-[11px] font-bold text-primary"><ShieldCheck className="h-4 w-4" />Accès restreint</div></div></header><div className="px-5 pt-7">{children}</div></div>; }
function TotalLine({ label, value, strong = false }: { label: string; value: string; strong?: boolean }) { return <div className={`flex justify-between gap-3 ${strong ? "text-base font-extrabold text-[#1e6051]" : "text-slate-600"}`}><span>{label}</span><span className="font-mono">{value}</span></div>; }
