import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { downloadPdfFromElement } from "@/lib/pdf";
import { trpc } from "@/lib/trpc";
import { formatGnf, type PaymentMethod } from "@shared/billing";
import { AlertTriangle, ChevronLeft, Download, FilePenLine, Loader2, Plus, Printer, WalletCards } from "lucide-react";
import { FormEvent, useState } from "react";
import { toast } from "sonner";
import { useLocation, useParams } from "wouter";
import { humanStatus } from "./DocumentsPage";

const paymentMethods: Record<PaymentMethod, string> = { especes: "Espèces", virement: "Virement", cheque: "Chèque", mobile_money: "Mobile Money", autre: "Autre" };

export default function DocumentPreviewPage() {
  const [, setLocation] = useLocation();
  const { id } = useParams<{ id: string }>();
  const documentId = Number(id);
  const { data: document, isLoading } = trpc.billing.documents.get.useQuery({ id: documentId || 1 });
  const utils = trpc.useUtils();
  const [isExporting, setIsExporting] = useState(false);
  const [showPayment, setShowPayment] = useState(false);
  const [paymentAmount, setPaymentAmount] = useState("");
  const [paymentDate, setPaymentDate] = useState(new Date().toISOString().slice(0, 10));
  const [method, setMethod] = useState<PaymentMethod>("virement");
  const [reference, setReference] = useState("");
  const [paymentNotes, setPaymentNotes] = useState("");
  const addPayment = trpc.billing.payments.create.useMutation({
    onSuccess: result => {
      utils.billing.documents.get.invalidate({ id: documentId });
      utils.billing.documents.list.invalidate();
      utils.billing.dashboard.invalidate();
      setPaymentAmount(""); setReference(""); setPaymentNotes(""); setShowPayment(false);
      toast.success(result.status === "paye" ? "Facture intégralement réglée." : "Paiement partiel enregistré.");
    },
    onError: error => toast.error(error.message),
  });

  if (isLoading) return <DashboardLayout><div className="flex min-h-[60vh] items-center justify-center"><Loader2 className="h-6 w-6 animate-spin text-primary" /></div></DashboardLayout>;
  if (!document) return <DashboardLayout><div className="p-8 text-center text-muted-foreground">Ce document est introuvable.</div></DashboardLayout>;

  const isInvoice = document.kind === "facture";
  const kindLabel = isInvoice ? "Facture" : "Devis";
  async function downloadPdf() { setIsExporting(true); try { await downloadPdfFromElement("lucepress-print-document", document?.number ?? "document-lucepress"); toast.success("Le PDF a été téléchargé."); } catch { toast.error("Le PDF n’a pas pu être généré. Réessayez dans un instant."); } finally { setIsExporting(false); } }
  function submitPayment(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const amount = Number(paymentAmount); if (!Number.isInteger(amount) || amount <= 0) return toast.error("Saisissez un montant entier positif en GNF."); if (!document) return; addPayment.mutate({ documentId: document.id, amount, paidAt: paymentDate, method, reference: reference || undefined, notes: paymentNotes || undefined }); }

  return <DashboardLayout><div className="mx-auto max-w-6xl"><header className="mb-6 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between print:hidden"><button onClick={() => setLocation(isInvoice ? "/factures" : "/devis")} className="flex items-center gap-1 text-xs font-bold text-primary hover:underline"><ChevronLeft className="h-3.5 w-3.5" />Retour à la liste</button><div className="flex flex-wrap gap-2"><Button variant="outline" onClick={() => setLocation(`/documents/${document.id}/edit`)} className="h-10 rounded-xl border-border font-bold"><FilePenLine className="mr-2 h-4 w-4" />Modifier</Button><Button variant="outline" onClick={() => window.print()} className="h-10 rounded-xl border-border font-bold"><Printer className="mr-2 h-4 w-4" />Imprimer</Button><Button onClick={downloadPdf} disabled={isExporting} className="h-10 rounded-xl bg-primary font-bold text-primary-foreground"><Download className="mr-2 h-4 w-4" />{isExporting ? "Génération…" : "Télécharger PDF"}</Button></div></header>
    {isInvoice && document.isOverdue && <div className="mb-5 flex items-start gap-3 rounded-2xl border border-red-300 bg-red-50 p-4 text-red-900 print:hidden"><AlertTriangle className="mt-0.5 h-5 w-5 shrink-0" /><div><p className="text-sm font-extrabold">Facture en retard</p><p className="mt-1 text-sm leading-5">L’échéance est dépassée et il reste <strong>{formatGnf(document.balanceDue)}</strong> à encaisser. Enregistrez un règlement ou relancez le client.</p></div></div>}
    {isInvoice && <section className="card-shadow mb-6 overflow-hidden rounded-2xl border border-border bg-card print:hidden"><div className="flex flex-col gap-4 border-b border-border p-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-secondary text-primary"><WalletCards className="h-5 w-5" /></div><div><h2 className="text-sm font-extrabold">Règlements</h2><p className="mt-1 text-xs text-muted-foreground">Suivi des paiements reçus et du solde restant.</p></div></div>{document.balanceDue > 0 && <Button onClick={() => setShowPayment(value => !value)} className="h-10 rounded-xl bg-primary font-bold text-primary-foreground"><Plus className="mr-2 h-4 w-4" />Enregistrer un paiement</Button>}</div><div className="grid divide-y divide-border sm:grid-cols-3 sm:divide-x sm:divide-y-0"><Metric label="Montant TTC" value={formatGnf(document.total)} /><Metric label="Déjà encaissé" value={formatGnf(document.paidAmount)} tone="text-emerald-700" /><Metric label="Solde dû" value={formatGnf(document.balanceDue)} tone={document.balanceDue > 0 ? "text-primary" : "text-emerald-700"} /></div>{showPayment && <form onSubmit={submitPayment} className="border-t border-border bg-muted/25 p-5"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><PaymentField label="Montant reçu (GNF) *"><input type="number" min="1" max={document.balanceDue} value={paymentAmount} onChange={e => setPaymentAmount(e.target.value)} required /></PaymentField><PaymentField label="Date du règlement *"><input type="date" value={paymentDate} onChange={e => setPaymentDate(e.target.value)} required /></PaymentField><PaymentField label="Mode de règlement"><select value={method} onChange={e => setMethod(e.target.value as PaymentMethod)}>{Object.entries(paymentMethods).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select></PaymentField><PaymentField label="Référence"><input value={reference} onChange={e => setReference(e.target.value)} placeholder="N° virement, reçu…" /></PaymentField></div><PaymentField label="Note interne"><textarea value={paymentNotes} onChange={e => setPaymentNotes(e.target.value)} placeholder="Précision éventuelle sur ce règlement" /></PaymentField><div className="mt-4 flex flex-wrap items-center justify-between gap-3"><p className="text-xs text-muted-foreground">Le montant ne peut pas dépasser le solde de <strong>{formatGnf(document.balanceDue)}</strong>.</p><div className="flex gap-2"><Button type="button" variant="outline" onClick={() => setShowPayment(false)} className="h-10 rounded-xl border-border font-bold">Annuler</Button><Button type="submit" disabled={addPayment.isPending} className="h-10 rounded-xl bg-primary font-bold text-primary-foreground">{addPayment.isPending && <Loader2 className="mr-2 h-4 w-4 animate-spin" />}Valider le règlement</Button></div></div></form>}{document.payments.length > 0 && <div className="divide-y divide-border">{document.payments.map(payment => <div key={payment.id} className="flex flex-wrap items-center justify-between gap-3 px-5 py-3.5"><div><p className="text-sm font-bold">{paymentMethods[payment.method as PaymentMethod]}</p><p className="mt-1 text-xs text-muted-foreground">Le {new Date(payment.paidAt).toLocaleDateString("fr-GN")}{payment.reference ? ` · ${payment.reference}` : ""}</p></div><p className="font-mono text-sm font-extrabold text-emerald-700">+ {formatGnf(payment.amount)}</p></div>)}</div>}</section>}
    <article id="lucepress-print-document" className="card-shadow mx-auto max-w-[210mm] bg-white text-[#183a35] print:shadow-none"><div className="border-b-[10px] border-[#153f38] p-8 sm:p-12"><div className="flex flex-col justify-between gap-8 sm:flex-row"><div className="flex items-center gap-4"><img src="/manus-storage/lucepress-emblem_bfa24e8e.png" alt="Emblème Lucepress" className="h-16 w-16 shrink-0 rounded-2xl bg-[#edf5f0] p-2" /><div><p className="font-editorial text-4xl font-semibold tracking-tight">Lucepress</p><p className="mt-2 text-[11px] font-extrabold uppercase tracking-[0.18em] text-[#4b746d]">BTP · Forage · Solutions terrain</p></div></div><div className="sm:text-right"><p className="font-editorial text-3xl font-semibold">{kindLabel}</p><p className="mt-2 font-mono text-sm font-bold text-[#1e6051]">{document.number}</p><p className="mt-1 text-xs text-slate-500">Statut : <span className="capitalize">{humanStatus(document.status)}</span></p></div></div></div><div className="grid gap-8 p-8 sm:grid-cols-2 sm:p-12"><section><p className="text-[10px] font-extrabold uppercase tracking-[0.18em] text-[#4b746d]">Destinataire</p><p className="mt-3 text-sm font-extrabold">{document.clientName}</p>{document.contactName && <p className="mt-1 text-sm">{document.contactName}</p>}{document.clientAddress && <p className="mt-1 max-w-xs whitespace-pre-line text-sm leading-6 text-slate-600">{document.clientAddress}</p>}{document.clientEmail && <p className="mt-1 text-sm text-slate-600">{document.clientEmail}</p>}</section><section className="sm:text-right"><p className="text-[10px] font-extrabold uppercase tracking-[0.18em] text-[#4b746d]">Informations</p><dl className="mt-3 space-y-1.5 text-sm"><InfoRow label="Date d’émission" value={new Date(document.issueDate).toLocaleDateString("fr-GN")} />{document.kind === "devis" && document.validUntil && <InfoRow label="Valide jusqu’au" value={new Date(document.validUntil).toLocaleDateString("fr-GN")} />}{isInvoice && document.dueDate && <InfoRow label="Échéance" value={new Date(document.dueDate).toLocaleDateString("fr-GN")} />}{document.projectName && <InfoRow label="Chantier" value={document.projectName} />}</dl></section></div><div className="px-8 pb-8 sm:px-12"><div className="overflow-hidden rounded-xl border border-slate-200"><table className="w-full border-collapse text-left text-xs"><thead className="bg-[#eef5f1] text-[#28534b]"><tr><th className="px-3 py-3 font-extrabold sm:px-4">Désignation</th><th className="px-2 py-3 text-right font-extrabold">Qté</th><th className="hidden px-2 py-3 text-right font-extrabold sm:table-cell">PU (GNF)</th><th className="px-3 py-3 text-right font-extrabold sm:px-4">Total (GNF)</th></tr></thead><tbody>{document.lines.map(line => <tr key={line.id} className="border-t border-slate-200"><td className="px-3 py-3.5 leading-5 sm:px-4">{line.description}<span className="ml-1 text-slate-400">({line.unit})</span></td><td className="px-2 py-3.5 text-right font-mono">{Number(line.quantity)}</td><td className="hidden px-2 py-3.5 text-right font-mono sm:table-cell">{new Intl.NumberFormat("fr-GN").format(line.unitPrice)}</td><td className="px-3 py-3.5 text-right font-mono font-bold sm:px-4">{new Intl.NumberFormat("fr-GN").format(line.lineTotal)}</td></tr>)}</tbody></table></div><div className="ml-auto mt-5 max-w-xs space-y-2 border-b border-[#153f38] pb-4 text-sm"><div className="flex justify-between text-slate-600"><span>Sous-total</span><span className="font-mono">{formatGnf(document.subtotal)}</span></div><div className="flex justify-between text-slate-600"><span>Taxes</span><span className="font-mono">{formatGnf(document.taxTotal)}</span></div><div className="mt-3 flex justify-between text-base font-extrabold"><span>Total TTC</span><span className="font-mono text-[#1e6051]">{formatGnf(document.total)}</span></div>{isInvoice && <><div className="flex justify-between text-slate-600"><span>Déjà encaissé</span><span className="font-mono">{formatGnf(document.paidAmount)}</span></div><div className="flex justify-between font-extrabold"><span>Solde dû</span><span className="font-mono text-[#1e6051]">{formatGnf(document.balanceDue)}</span></div></>}</div>{document.notes && <section className="mt-8 rounded-xl bg-[#f8faf9] p-4"><p className="text-[10px] font-extrabold uppercase tracking-[0.18em] text-[#4b746d]">Conditions et notes</p><p className="mt-2 whitespace-pre-line text-sm leading-6 text-slate-600">{document.notes}</p></section>}<footer className="mt-12 border-t border-slate-200 pt-5 text-center text-[10px] font-medium tracking-wide text-slate-400">Document émis par Lucepress · Montants exprimés en francs guinéens (GNF)</footer></div></article></div></DashboardLayout>;
}

function Metric({ label, value, tone = "text-foreground" }: { label: string; value: string; tone?: string }) { return <div className="p-5"><p className="text-[10px] font-extrabold uppercase tracking-[0.15em] text-muted-foreground">{label}</p><p className={`mt-2 font-mono text-lg font-extrabold ${tone}`}>{value}</p></div>; }
function PaymentField({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block text-xs font-extrabold">{label}<div className="mt-2 [&_input]:h-10 [&_input]:w-full [&_input]:rounded-lg [&_input]:border [&_input]:border-border [&_input]:bg-card [&_input]:px-3 [&_input]:text-sm [&_select]:h-10 [&_select]:w-full [&_select]:rounded-lg [&_select]:border [&_select]:border-border [&_select]:bg-card [&_select]:px-3 [&_select]:text-sm [&_textarea]:mt-2 [&_textarea]:min-h-20 [&_textarea]:w-full [&_textarea]:rounded-lg [&_textarea]:border [&_textarea]:border-border [&_textarea]:bg-card [&_textarea]:p-3 [&_textarea]:text-sm">{children}</div></label>; }
function InfoRow({ label, value }: { label: string; value: string }) { return <div className="flex justify-between gap-4 sm:justify-end"><dt className="text-slate-500">{label}</dt><dd className="font-bold">{value}</dd></div>; }
